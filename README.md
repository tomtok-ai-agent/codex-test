# Date Poet

A simple console application that generates a short poem about today's date using the OpenAI API.

## Usage

1. Ensure `openai` and `click` packages are installed.
2. Set the environment variable `OPENAI_API_KEY` with your API key.
3. Run the application with `poetry run date-poet` or `python -m date_poet.cli`.
